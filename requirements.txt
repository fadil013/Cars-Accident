ultralytics>=8.3.0
opencv-python>=4.8.0
numpy>=1.24.0
# torch is pulled in by ultralytics (install the CUDA build for GPU)
# ffmpeg must be available on PATH (used to transcode the annotated output to H.264)
